```
function shellSort(arr : number[]) : void
{
    let i,j;
    let increment = arr.length-1;
    do 
    {
        increment = increment/3 + 1;
        for (i=increment+1; i<arr.length; i++) {
            if (arr[i] < arr[i-increment]) {
                arr[0] = arr[i];
                for (j=i-increment; j>0 && arr[0]<r[j]; j-=increment) {
                    arr[j+increment] = r[j];
                }
            }
            arr[j+increment] = r[0];
        }
    }
    while (increment > 1)  
}
```